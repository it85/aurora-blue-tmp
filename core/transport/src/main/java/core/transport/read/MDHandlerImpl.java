package core.transport.read;

import common.data.marketdata.L3Quote;
import common.messaging.MDHandler;

// TODO: come up with a better name than "...Impl"
final class MDHandlerImpl implements MDHandler {

    @Override
    public void handle(L3Quote quote) {
    }
}
